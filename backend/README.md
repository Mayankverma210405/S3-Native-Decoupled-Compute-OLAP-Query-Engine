# Backend

FastAPI backend for the S3 Native Decoupled Compute OLAP Query Engine.

This service will be responsible for:

* AWS S3 integration
* Presigned URL generation
* DuckDB query execution
* Metadata management
* Engineering metrics
* REST API endpoints
